name: Publish to NuGet

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
          
      - name: Restore
        run: dotnet restore ProxyForge.sln
        
      - name: Build All
        run: dotnet build ProxyForge.sln -c Release --no-restore
        
      - name: Pack Core
        run: dotnet pack src/ProxyForge.Core/ProxyForge.Core.csproj -c Release -o ./nupkg --no-build
        
      - name: Pack WinForms
        run: dotnet pack src/ProxyForge.WinForms/ProxyForge.WinForms.csproj -c Release -o ./nupkg --no-build
        
      - name: Push to NuGet
        run: dotnet nuget push ./nupkg/*.nupkg --source https://api.nuget.org/v3/index.json --skip-duplicate
